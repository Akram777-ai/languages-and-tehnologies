📘 README.md — Система учёта студентов

🎓 Система учёта студентов

Консольное приложение, демонстрирующее применение принципов объектно-ориентированного программирования (ООП): инкапсуляция, работа с объектами, взаимодействие между классами, сохранение данных в формате JSON.

⸻

📌 Цель работы
	•	Научиться создавать собственные классы.
	•	Освоить инкапсуляцию и работу с приватными атрибутами.
	•	Реализовать взаимодействие объектов между собой.
	•	Сформировать навыки модульного и структурированного программирования.
	•	Научиться сохранять данные в JSON-файлы.

⸻

📌 Функционал программы

Программа реализует две сущности:

🧑‍🎓 Класс Student

Содержит:
	•	__name — имя студента (приватный атрибут)
	•	__group — учебная группа
	•	__gpa — GPA (средний балл)

Методы:
	•	__init__(name, group, gpa) — конструктор
	•	display_info() — вывод информации
	•	update_gpa(new_gpa) — изменение GPA
	•	get_name(), get_group(), get_gpa() — геттеры
	•	set_gpa(), set_group() — сеттеры
	•	to_dict() — преобразование объекта в формат JSON
	•	from_dict() — восстановление объекта из JSON

⸻

🏫 Класс Group

Хранит список студентов.

Методы:
	•	add_student(student) — добавить студента
	•	remove_student(name) — удалить по имени
	•	show_all() — вывести всех студентов
	•	get_top_students(threshold) — топ-студенты по GPA
	•	save_to_file(filename) — сохранение списка в JSON
	•	load_from_file(filename) — загрузка списка из JSON



	